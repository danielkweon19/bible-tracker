# Project Instructions

- GitHub `origin/main` is the source of truth for this project and its production deployments.
- Production changes must be committed and pushed to `main` so GitHub Actions deploys them.
- Do not leave changes deployed directly from a local workspace without committing and pushing the exact same changes to `main`.
- At the end of deployment work, explicitly report whether the changes were committed and pushed.
